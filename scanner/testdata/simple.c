#include <stdio.h>
#include "stdio.h"
#define M(a, ...) #a ## #__VA_ARGS__
%:define C 12345

int main() <%
    int a\
b =  /*comment */                   10;
    char a = '\123';
    int num = .213e-2; // comment
    if (a != 10 && ab > 10) {
        printf("v", a);
    }
    M(a, b, c, d, e<:]);
}
